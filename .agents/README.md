# Local Agents and Skills

This folder contains local AI-agent assets for the AppGro rewrite.

## Structure

- `agents.md`: local catalog of suggested specialized agents
- `skills/`: reusable skill packs with operational guidance

## Current Skills

- `mermaid-diagrams` (pre-existing)
- `appgro-domain`
- `astro-fastapi-migration`
- `rewrite-documentation-specs`

## Usage Pattern

1. Read repository context files first (`AGENTS.md`, `.github/copilot-instructions.md`).
2. Load the most relevant skill before implementation.
3. Keep generated output aligned with domain language and planned architecture.
